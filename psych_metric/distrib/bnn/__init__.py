from psych_metric.distrib.bnn import bnn_mcmc
from psych_metric.distrib.bnn import mcmc
# TODO need to fix the namespace such that only what I want from each file is
# pulled into the package, not tensorflow as tf being accessible from these
# files.
